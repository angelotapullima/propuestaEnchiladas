import 'package:flutter/material.dart';

class Usuario2 extends StatelessWidget {
  const Usuario2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('usuario'),
      ),
    );
  }
}
