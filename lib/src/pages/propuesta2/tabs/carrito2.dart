import 'package:flutter/material.dart';

class Carrito2 extends StatelessWidget {
  const Carrito2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Carrito2'),
      ),
    );
  }
}
