import 'package:flutter/material.dart';

class Usuario1 extends StatelessWidget {
  const Usuario1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('usuario'),
      ),
    );
  }
}
